- project:
    check:
      jobs:
        - linters
    gate:
      queue: thoth-station/core
      jobs:
        - linters
    kebechet-auto-gate:
      queue: thoth-station/core
      jobs:
        - linters
    post:
      jobs:
        - trigger-build:
            vars:
              webhook_url: https://paas.upshift.redhat.com/oapi/v1/namespaces/thoth-test-core/buildconfigs/graph-refresh-job/webhooks/
    release:
      jobs:
        - upload-pypi-sesheta
